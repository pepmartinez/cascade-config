{
  "name": "cascade-config",
  "description": "hierarchical config for node.js (env, argv, files, dirs) with inline var substitution",
  "version": "1.0.0",
  "dependencies": {
    "lodash": "4.17.10",
    "async": "2.6.1",
    "klaw": "2.1.1",
    "mongodb": "2.2.28",
    "pupa": "1.0.0",
    "minimist": "1.2.0"
  },
  "devDependencies": {
    "mocha": "5.2.0",
    "should": "13.2.1"
  },
  "scripts": {
    "test": "mocha --reporter spec --check-leaks --bail test/"
  }
}
